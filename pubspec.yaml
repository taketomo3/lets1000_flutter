name: lets1000_android
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.1.0+2

environment:
  sdk: '>=2.19.3 <3.0.0'

dependencies:
cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  flutter_riverpod:
  fluttertoast: # トースト
  grouped_list: # ListViewをまとめる
  package_info_plus: # アプリのバージョン情報
  path:
  path_provider:
  sqflite:
  webview_flutter: ^4.0.6

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
  pedantic_mono: any

flutter:
  uses-material-design: true
